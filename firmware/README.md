# LOOM Node Firmware

Placeholder for the future ESP32 + LoRa + BLE firmware project.

This directory intentionally does not contain a `package.json`; firmware is not a Node workspace.
Phase 8 will choose the concrete Arduino or PlatformIO project structure. The existing
`edge-device/` sketches are retained as historical prototypes and are not migrated in phases 0-3.
