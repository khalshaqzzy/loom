# LOOM Decision Tree

Placeholder for the future mobile-consumable decision-tree compression module.

This directory intentionally does not contain a `package.json` yet. It will become an npm workspace
when Phase 9 defines and implements the canonical compression taxonomy.
